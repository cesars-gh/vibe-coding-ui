# Migration to NPM package

Let's udpate this project to a second iteration to make it an npm package installable as a dev dependency in any node.js package.
Analyze the existing code and deeply think about the changes needed to migrate to npm package.

## Goal
Any package can install `vibe-coding` and run `pnpm vibe-coding start` to kick off both `agent-api` and `frontend` packages in a unified server so when the user loads `localhost:8000` a new Claude Code session is spawned with the cwd as the project's root folder.


## Plan
TODO: complete the plan and the implementation checklist

### Implementation
- [ ] Task 1: details
- [ ] Task 2: details

**Notes:** When you are implementing the code changes, mark the task as soon as it is completed.
