{
  "name": "vibe-coding-ui",
  "version": "0.1.4",
  "type": "module",
  "description": "AI-powered coding assistant UI — install globally and run `vibe-coding-ui start`",
  "bin": {
    "vibe-coding-ui": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "dist/",
    "packages/agent-api/dist/"
  ],
  "scripts": {
    "build": "pnpm --filter frontend build",
    "build:api": "pnpm --filter agent-api build",
    "prepublishOnly": "pnpm build:api && pnpm build",
    "dev": "pnpm --filter agent-api dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@hono/node-server": "^1.14.0",
    "@hono/node-ws": "^1.1.0",
    "dotenv": "^17.3.1",
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
